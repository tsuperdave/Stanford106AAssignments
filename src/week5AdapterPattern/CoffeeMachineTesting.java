package week5AdapterPattern;

import static org.junit.Assert.assertEquals;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.Test;
// import org.junit.jupiter.api.Test;

class CoffeeMachineTesting {
	/*
	@Test
	void testOldCoffeeMachine() {
		OldCoffeeMachine machine = new OldCoffeeMachine();
		
		assertEquals("Option A was Selected from Old Machine", machine.selectA());
		assertEquals("Option B was Selected from Old Machine", machine.selectB());
	}

	@Test
	void testCoffeeMachineInterface() {
		CoffeeMachineInterface interface = new CoffeeMachineInterface();
		
		assertEquals("Option A was Selected from Old Machine", machine.selectA());
		assertEquals("Option B was Selected from Old Machine", machine.selectB());
	}
	*/
}
